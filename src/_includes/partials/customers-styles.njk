{# Page-scoped CSS for /customers (EN + DE). #}
<style>
    .cust-hero {
        background: var(--v-amber);
        font-family: var(--v-sans);
        position: relative;
        overflow: hidden;
    }
    .cust-hero-inner {
        max-width: 1280px;
        margin: 0 auto;
        padding: 100px 40px 100px;
        text-align: left;
    }
    .cust-hero-headline {
        font-family: var(--v-display);
        font-size: clamp(64px, 9vw, 140px);
        line-height: 0.86;
        letter-spacing: -0.05em;
        font-weight: 900;
        color: var(--v-bg-dark);
        margin: 0 0 48px;
        text-transform: uppercase;
    }
    .cust-hero-headline span { display: block; }
    .cust-hero-subline {
        font-size: 18px;
        line-height: 1.55;
        color: rgba(14, 17, 22, 0.72);
        max-width: 640px;
        margin: 0;
        font-weight: 500;
    }
    @media (max-width: 768px) {
        .cust-hero-inner { padding: 60px 24px 60px; }
        .cust-hero-headline { letter-spacing: -0.035em; }
        .cust-hero-subline { font-size: 16px; }
    }

    .logo-wall {
        background: var(--v-bg);
        font-family: var(--v-sans);
    }
    .logo-wall-inner {
        max-width: 1240px;
        margin: 0 auto;
        padding: 100px 40px 100px;
    }
    .logo-wall-eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        font-size: 11.5px;
        font-weight: 700;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--v-amber-deep);
        margin-bottom: 20px;
    }
    .logo-wall-eyebrow::before {
        content: '';
        width: 24px;
        height: 1.5px;
        background: var(--v-amber);
    }
    .logo-wall-headline {
        font-family: var(--v-display);
        font-size: clamp(34px, 4.2vw, 60px);
        line-height: 1.02;
        letter-spacing: -0.035em;
        font-weight: 900;
        color: var(--v-ink);
        margin: 0 0 72px;
        max-width: 820px;
    }
    .logo-wall-headline em { font-style: normal; color: var(--v-amber); }
    .logo-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1px;
        background: var(--v-line);
        border: 1px solid var(--v-line);
    }
    .logo-cell {
        background: var(--v-bg-card);
        aspect-ratio: 2 / 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 28px 32px;
    }
    .logo-cell img {
        max-width: 100%;
        max-height: 64px;
        width: auto;
        height: auto;
        object-fit: contain;
        display: block;
        filter: grayscale(1);
        opacity: 0.75;
        transition: filter 0.2s ease, opacity 0.2s ease;
    }
    .logo-cell:hover img {
        filter: grayscale(0);
        opacity: 1;
    }
    @media (max-width: 1024px) {
        .logo-wall-inner { padding: 60px 32px 60px; }
        .logo-grid { grid-template-columns: repeat(3, 1fr); }
    }
    @media (max-width: 640px) {
        .logo-wall-inner { padding: 48px 20px 48px; }
        .logo-wall-headline { font-size: clamp(24px, 7vw, 32px); margin-bottom: 32px; }
        .logo-grid { grid-template-columns: repeat(2, 1fr); }
    }

    .quotes {
        background: var(--v-bg);
        font-family: var(--v-sans);
    }
    .quotes-inner { max-width: 1240px; margin: 0 auto; padding: 100px 40px 100px; }
    .quotes-header { margin-bottom: 56px; max-width: 820px; }
    .quotes-eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        font-size: 11.5px;
        font-weight: 700;
        letter-spacing: 0.18em;
        text-transform: uppercase;
        color: var(--v-amber-deep);
        margin-bottom: 20px;
    }
    .quotes-eyebrow::before {
        content: '';
        width: 24px;
        height: 1.5px;
        background: var(--v-amber);
    }
    .quotes-headline {
        font-family: var(--v-display);
        font-size: clamp(34px, 4.2vw, 60px);
        line-height: 1.02;
        letter-spacing: -0.035em;
        font-weight: 900;
        color: var(--v-ink);
        margin: 0;
    }
    .quotes-headline em { font-style: normal; color: var(--v-amber); }
    .quotes-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
    }
    .quote-card {
        background: var(--v-bg-card);
        border: 1px solid var(--v-line);
        border-top: 3px solid var(--v-amber);
        padding: 36px 36px 32px;
        display: flex;
        flex-direction: column;
    }
    .quote-mark {
        font-family: var(--v-display);
        font-size: 56px;
        line-height: 0.6;
        color: var(--v-amber);
        margin-bottom: 14px;
        font-weight: 900;
    }
    .quote-text {
        font-size: 17px;
        line-height: 1.55;
        color: var(--v-ink);
        font-weight: 500;
        margin: 0 0 32px;
        flex-grow: 1;
        letter-spacing: -0.005em;
    }
    .quote-attribution {
        display: flex;
        flex-direction: column;
        gap: 4px;
        padding-top: 20px;
        border-top: 1px solid var(--v-line-soft);
    }
    .quote-name {
        font-size: 14.5px;
        font-weight: 700;
        color: var(--v-ink);
        letter-spacing: -0.005em;
    }
    .quote-role { font-size: 13px; color: var(--v-ink-2); font-weight: 500; }
    .quote-company {
        font-size: 11px;
        font-weight: 700;
        letter-spacing: 0.14em;
        text-transform: uppercase;
        color: var(--v-ink-3);
        margin-top: 4px;
    }
    @media (max-width: 900px) {
        .quotes-inner { padding: 60px 24px 60px; }
        .quotes-grid { grid-template-columns: 1fr; gap: 16px; }
        .quote-card { padding: 28px 24px 24px; }
    }

    .cust-cta { background: var(--v-bg-dark); font-family: var(--v-sans); }
    .cust-cta-inner { max-width: 1240px; margin: 0 auto; padding: 100px 40px 100px; text-align: left; }
    .cust-cta-headline {
        font-family: var(--v-display);
        font-size: clamp(38px, 5vw, 72px);
        line-height: 1.0;
        letter-spacing: -0.035em;
        font-weight: 900;
        color: #fff;
        margin: 0 0 32px;
    }
    .cust-cta-headline em { font-style: normal; color: var(--v-amber); }
    .cust-cta-sub {
        font-size: 18px;
        line-height: 1.55;
        color: rgba(255, 255, 255, 0.68);
        max-width: 560px;
        margin: 0 0 36px;
        font-weight: 500;
    }
    .cust-cta-btn {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 16px 28px;
        background: var(--v-amber);
        color: var(--v-bg-dark);
        text-decoration: none;
        font-size: 15px;
        font-weight: 700;
        letter-spacing: -0.005em;
        transition: background 0.15s, color 0.15s;
    }
    .cust-cta-btn:hover { background: #fff; color: var(--v-bg-dark); }
    @media (max-width: 640px) {
        .cust-cta-inner { padding: 60px 24px 60px; }
        .cust-cta-sub { font-size: 16px; }
    }
</style>
